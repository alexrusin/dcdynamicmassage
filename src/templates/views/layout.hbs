<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="/css/main.css">
    {{#if title}} 
    <title>{{ title }}</title>
    <meta name="description" content="{{ metaDescription }}">
    {{else}}
    <title>DC Dynamic Massage</title>
    <meta name="description" content="Your body needs maintenance just like everything else. Massage therapy is not just a luxury, it's a key piece of your health and wellness plan">
    {{/if}}
    {{#if jsonLd}}
    {{{jsonLd}}}
    {{/if}}
</head>

<body class="text-gray-800">
    <div id="app" class="flex flex-col min-h-screen">
        <navbar></navbar>

        <main class="flex-grow">
            {{{ body }}}
        </main>

       {{> footer}}
    </div>
    <script src="/js/app.js"></script>
</body>

</html>